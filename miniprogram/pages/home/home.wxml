<!-- home.wxml -->
<view class="container">
  <!-- 搜索框 -->
  <view class="search-container">
    <input 
      type="text" 
      placeholder="请输入关键词" 
      class="search-input" 
      bindinput="onSearchInput"
      bindconfirm="onSearch"
      confirm-type="search"
      value="{{searchKeyword}}"
    />
  </view>

  <!-- 轮播图 -->
  <view class="carousel-container">
    <swiper indicator-dots="{{false}}" autoplay="true" interval="5000" duration="1000" bindchange="onSwiperChange">
      <swiper-item wx:for="{{carouselImages}}" wx:key="id" bindtap="onCarouselTap" data-article_url="{{item.article_url}}">
        <image src="{{item.image_url}}" class="carousel-image" />
        <view class="carousel-overlay" wx:if="{{item.title}}">
          <text class="carousel-title">{{item.title}}</text>
        </view>
      </swiper-item>
    </swiper>
    <!-- 自定义指示器 -->
    <view class="carousel-indicator" wx:if="{{carouselImages.length > 0}}">
      {{current + 1}}/{{carouselImages.length}}
    </view>
  </view>

  <!-- 子菜单 -->
  <view class="submenu-container">
    <view class="submenu-item" wx:for="{{submenus}}" wx:key="id" bindtap="onMenuTap" data-id="{{item.id}}" data-content="{{item.content}}">
      <image src="{{item.icon}}" class="submenu-icon" />
      <text class="submenu-text">{{item.title}}</text>
    </view>
  </view>


  <!-- 案例分享列表 -->
  <view class="case-studies-container">
    <view class="section-title">案例分享</view>
    <view class="case-study-item" wx:for="{{caseStudies}}" wx:key="id" bindtap="navigateToCaseStudy" data-id="{{item.id}}">
      <image src="{{item.image_url}}" class="case-study-image" />
      <view class="case-study-content">
        <text class="case-study-title">{{item.title}}</text>
        <view class="case-study-meta">
          <text class="case-study-date">{{item.publish_date}}</text>
          <view class="case-study-stats">
            <view class="case-study-view">
              <image src="/images/icon/yanjing_1.png" class="view-icon" />
              <text>{{item.view_count}}</text>
            </view>
          </view>
        </view>
      </view>
    </view>
    <view class="load-more-container">
      <button class="load-more-button" bindtap="navigateToCaseStudyList">加载更多</button>
    </view>
  </view>
</view>